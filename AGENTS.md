# xchan

Product decisions live in `PRODUCT.md`. Treat that file as source of truth.

- Do not scaffold or implement until `PRODUCT.md` is filled in and the user asks to proceed.
- Prefer `@PRODUCT.md` over restating the brief in chat.
- Do not invent stack, scope, or features for blank sections — ask instead.
- Never commit secrets or put API keys in client code.
